import React from 'react';
import { PlayCircleFilled } from '@material-ui/icons';

const Instagram = (props) => (
  <PlayCircleFilled style={{ transform: 'rotate(180deg)' }} {...props} />
);

export default Instagram;
